import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function ResponsePage() {
  return (
    <ScreenPlaceholder
      module="Response"
      title="Response Dashboard"
      phase="Phase 3"
      blurb="Metric tiles (time-to-freeze, recovery rate), trend sparkline, and the cases table — the impact view of the whole pipeline."
    />
  );
}
