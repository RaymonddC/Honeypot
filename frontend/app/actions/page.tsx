import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function ActionsPage() {
  return (
    <ScreenPlaceholder
      module="Uncover"
      title="Action Panel"
      phase="Phase 3"
      blurb="One-click generation of the freeze-request PDF, goAML LTKM draft, and multi-agency alert — with human-gated dispatch (POC mode = mock sink)."
    />
  );
}
