import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function HoneypotPage() {
  return (
    <ScreenPlaceholder
      module="Infiltrate"
      title="Honeypot"
      phase="Phase 4"
      blurb="Live scam-session console: chat transcript with inline entity extraction, extracted-entities panel with confidence, chain-of-custody hashes, and the voice-call indicator."
    />
  );
}
