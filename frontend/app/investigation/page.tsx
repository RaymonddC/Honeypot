import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function InvestigationPage() {
  return (
    <ScreenPlaceholder
      module="Takedown"
      title="Investigation"
      phase="Phase 1"
      blurb="Wallet search → risk-colored transaction graph (Cytoscape), 12-feature wallet detail with risk gauge, fired typology patterns, and the Glass Box reasoning trace."
    />
  );
}
